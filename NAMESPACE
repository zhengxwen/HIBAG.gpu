# Load the shared object
useDynLib(HIBAG.gpu,
	gpu_init_proc, gpu_set_val, gpu_build_kernel
)

# Export function names
exportPattern("^hla")
export(.onAttach)

# Imports
import(methods)
import(HIBAG)
importFrom(stats, predict)
